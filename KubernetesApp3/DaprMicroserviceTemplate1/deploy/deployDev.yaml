# THIS FILE IS AUTOGENERATED BY A TOOL
# IF YOU EDIT IT ANY CHANGES YOU MAKE WILL BE LOST

apiVersion: apps/v1
kind: Deployment
metadata:
  name: kubernetes3app
  labels:
    app: kubernetes3
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kubernetes3
  template:
    metadata:
      labels:
        app: kubernetes3
      annotations:
        dapr.io/enabled: "true"
        dapr.io/id: "kubernetes3"
        dapr.io/port: "3000" 
        prometheus.io/scrape: 'true'
        prometheus.io/port:   '9090'
    spec:
      containers:
      - name: kubernetes3app
        image: adlacrdev.azurecr.io/kubernetes3:$(tag)
        ports:
        - containerPort: 3000
        imagePullPolicy: Always
        env:
        - name: "ASPNETCORE_ENVIRONMENT"
          value: "Development"

